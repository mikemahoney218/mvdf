#' @param object An object inheriting from [mvdf_obj] which will be used to
#' calculate the x, y, and z positions for each primitive. One primitive will be
#' created for each row of `mvdf(object)`.
